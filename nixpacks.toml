[variables]
HOSTNAME = '0.0.0.0'
PORT = '3000'

providers = ["node"]

[phases.install]
cmds = ['npm ci --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node .next/standalone/server.js'
